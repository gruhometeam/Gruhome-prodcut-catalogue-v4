The functional refactor is done, but the UI layout and styling were not applied. 
Do NOT rewrite the data logic again — only fix the visual structure, spacing, and presentation.

Please now transform the UI to match these visual rules:

------------------------------------
📌 LAYOUT FIXES (REQUIRED)
------------------------------------
1️⃣ Page width & margins:
- Main content must be centered with fixed 2% left and right margins
- Cards must be 100% width of the content area (NOT full screen)
- No breaking of layout on desktop or mobile

2️⃣ Display column headings dynamically as per rules:
- First heading → title (Serif)
- Second → subtitle line 1
- Third → subtitle line 2 (optional)
- Next two numeric headings → value grid
- Columns containing "gst" → badge
- Do NOT display full row headings at the top like a table

Remove the current giant heading row that prints ALL column names.

3️⃣ Card spacing & readability:
- Increase INNER padding to 16px
- Card border color #C8C2B8
- Font color #2B2B2B
- Rounded corners 12px
- Title font-size: 22–28px depending on screen size
- Subtitles smaller and lighter
- Grid values in two equal columns

4️⃣ No full width text blocks
Right now every value runs across full width — FIX so cards visually segment content.

------------------------------------
🎨 VISUAL POLISH
------------------------------------
- Background: #F4ECD8
- Text: #2B2B2B
- Accents: #8A9A5B
- Stop printing blank lines between values
- Space elements consistently (12–20px vertical rhythm)

------------------------------------
🪟 MODAL FIXES
------------------------------------
- Background overlay: rgba(0,0,0,0.45) (not pitch black)
- Modal panel: white body, rounded corners, padding 24px
- Title in serif, other text in Inter
- Close button top right

------------------------------------
🔍 SEARCH & FILTER VISIBILITY
------------------------------------
- Search bar full width of content area with rounded corners
- Filter dropdown MUST have opaque background so list items are readable
- Filter panel text should wrap instead of causing horizontal scroll

------------------------------------
🚫 DO NOT CHANGE
------------------------------------
❗ dynamic columns  
❗ filtering logic  
❗ search logic  
❗ product fetching  
❗ offline caching

------------------------------------
🎯 GOAL
------------------------------------
Make the UI visually match a premium product catalog. 
Cards should look like product tiles, not like rows of plain text.

When you're done:
- show the updated UI preview
- list which component files were changed
