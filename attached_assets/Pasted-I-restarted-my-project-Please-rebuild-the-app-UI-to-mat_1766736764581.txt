I restarted my project. Please rebuild the app UI to match these exact requirements.  
The data comes from a Google Sheets API URL and the structure must be *dynamic*:
- DO NOT hard-code column names
- Read the first row as headings and display row values below them
- If I rename a column in Sheets, the UI should update without code changes
- If a column is empty or N/A for a product, hide it

------------------------------------
🎨 **UI / Styling Requirements**
------------------------------------
Tone: **Minimal, premium, Gruhome aesthetic**  
Font: **Cormorant Garamond** for titles, **Inter** for body  
Colors:
- Background: `#F4ECD8` (warm cream)
- Text: `#2B2B2B` (charcoal)
- Border: `#C8C2B8` (light warm gray)
- Accent: `#8A9A5B` (sage green)

General spacing:
- Page margins: **2% left and right**
- Card internal padding: **16px all around**
- Rounded corners: **12px**
- Line spacing: **relaxed**, not cramped

------------------------------------
📌 **Page Layout**
------------------------------------
Top fixed header:
- Title: "Catalog"
- Search bar directly under title
- Multi-filter bar under search (dropdowns for selected columns)

Main product list:
- **Cards fill 96% width**, centered with **2% left & right margin**
- No horizontal scrolling anywhere
- Mobile: 1-column card grid
- Desktop: 2-column card grid
- Cards have shadow on hover & lift slightly

Inside each card:
- First column heading value = product title (largest serif)
- Next one or two headings = subtitle lines
- If a price/gst column exists, show it as a *sage badge*
- Show next two numeric columns formatted with `₹` if numeric
- Hide columns with empty or N/A values

------------------------------------
🔍 **Search**
------------------------------------
Search across all columns, case-insensitive  
The search bar must:
- span full width minus the page margins
- have subtle border and rounded corners
- include a left search icon

------------------------------------
🔽 **Filtering**
------------------------------------
Implement a MultiFilterBar:
- A dropdown for every column that has fewer than 30 unique values
- Dropdown panel has **cream background**, no transparency
- Long text must wrap, not overflow
- Selected value shows as “Column: Value”
- Button style: rounded, sage hover state

------------------------------------
🪟 **Product Modal**
------------------------------------
When a card is clicked:
- Show fullscreen modal with scrollable white panel centered
- Background dim: **rgba(0,0,0,0.45)** (not pitch black)
- Text color inside modal: **#2B2B2B**, headings #555
- Display each column/value on new line
- Hide empty/N/A fields
- Format numeric values as currency when appropriate
- Close button top-right

------------------------------------
🧠 **Dynamic Column Handling**
------------------------------------
Behavior:
- Read column headings dynamically
- ProductCards should display:
    1. first heading → card title (serif, large)
    2. second heading → subtitle line
    3. third heading → optional subtitle alt
    4. any heading with “gst” → GST badge
    5. next two headings after title/subtitles → numeric grid values
- Modal displays all remaining non-empty fields in order

------------------------------------
⚙️ **Other**
------------------------------------
- Cache data for offline browsing
- Do not display API URL on UI
- Smooth transitions between states
- No console errors
- Avoid UI flash on load by using skeleton loader
- Make sure dropdowns overlay correctly, not behind cards
- Fix dropdown readability: MUST have background so text below does not show through

------------------------------------
🏁 Final Output Expectations
------------------------------------
You should:
1. Rewrite necessary components (ProductCard, ProductModal, MultiFilterBar, SearchBar, useProducts)
2. Maintain all functionality from earlier versions:
    ✔ search  
    ✔ multi-filter  
    ✔ dynamic columns  
    ✔ product modal  
    ✔ currency formatting  
    ✔ offline caching  
3. Deliver a **polished & elegant** UI that is readable and balanced

------------------------------------
When you finish:
- Show me a preview
- Then list which files changed

Please begin now.
